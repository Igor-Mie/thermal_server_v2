# Thermal Server

To run the server manually, execute the following commands:

1.  **Start the server:**
    ```bash
    python3 thermal_server.py
    ```
2.  **Stop the server:**
    Press `Ctrl+C` in the terminal where the server is running.

This will start the Flask application and the camera/archive threads.